[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

# Frontend is pre-built and committed to api/static/ — no Node.js build needed.

[start]
cmd = "python main.py"
