[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm ci --prefix web"
]

[phases.build]
cmds = [
  "npm run build --prefix web",
  "cp -r web/out/. static/"
]

[start]
cmd = "python main.py"
