[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd web && npm ci"
]

[phases.build]
cmds = [
  "cd web && npm run build",
  "cp -r web/out/. static/"
]

[start]
cmd = "python main.py"
